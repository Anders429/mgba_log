[package]
name = "mgba_log_reporter"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = {version = "1.0.163", features = ["derive"]}
serde_json = "1.0.96"

[build-dependencies]
bindgen = "0.64.0"
cc = {version = "1.0.79", features = ["parallel"]}
